# Add this inside the existing `server { ... }` block for sirius.rusanoff.ru.
proxy_intercept_errors on;
error_page 502 503 504 /maintenance.html;

location = /maintenance.html {
    root /opt/sirius-plus/nginx;
    internal;
}
