# Add this inside the existing `server { ... }` block for sirius.rusanoff.ru.
proxy_intercept_errors on;
error_page 500 502 503 504 /maintenance.html;

# Add `auth_request /healthz;` to the existing `location / { ... }` block.
# It prevents Nginx from serving the old page while the application is stopping.

location = /maintenance.html {
    root /opt/sirius-plus/nginx;
    internal;
}

# The deploy command sets this before stopping the application.
location = /maintenance-state.js {
    root /opt/sirius-plus/nginx;
    default_type application/javascript;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
}

location = /healthz {
    proxy_pass http://127.0.0.1:8000/healthz;
    proxy_set_header Host $host;
    proxy_intercept_errors off;
    access_log off;
}
